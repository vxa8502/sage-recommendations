"""ESCI overlap query-bank package.

Import from the owning submodule:

- `cache` for corpus product-id cache helpers
- `config` for constants
- `labels` for relevance-label weight normalization
- `manifest` for ingestion manifest construction
- `policy` for test split assignment policy objects
- `rows` for canonical row building
- `summary` for row summaries
"""

from __future__ import annotations

__all__: list[str] = []
